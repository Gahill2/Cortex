{
  "framework": null,
  "installCommand": "npm install --include=dev",
  "buildCommand": "npm install --include=dev && ./node_modules/.bin/tsc -b && ./node_modules/.bin/vite build",
  "outputDirectory": "dist",
  "rewrites": [{ "source": "/(.*)", "destination": "/" }]
}
