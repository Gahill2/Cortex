# Copy into Vercel → Project → Settings → Environment Variables → Production.
# Required for split deploy (UI on Vercel, API on Railway). Redeploy after changes.
#
# Replace placeholders (no trailing slash on YOUR_RAILWAY_URL):
#   docs/cloud-deploy.md

VITE_API_BASE_URL=https://YOUR_RAILWAY_URL/api

# Optional — Canva Apps SDK display hints in Settings UI
# VITE_CANVA_APP_ID=
