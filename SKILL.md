# Design skill (moved)

The **design** Cursor skill lives at:

**[.cursor/skills/design/SKILL.md](.cursor/skills/design/SKILL.md)**

Invoke it in chat with `@design` or by asking for logo, CIP, banner, slide, icon, or social image work.

Setup: copy `.cursor/skills/design/.env.example` to `.env` and set `GEMINI_API_KEY`.
