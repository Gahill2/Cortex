{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities for the Cortex desktop shell.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
