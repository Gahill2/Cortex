# Cortex `/loop` (tracked copy)

Cursor skill: `.cursor/skills/cortex-loop/SKILL.md` (local; `.cursor/` is gitignored).

**Start in chat:** `/loop` or "start the improvement loop"

```bash
npm run loop                  # alias → dev:improve-loop (2m wakes)
npm run dev:improve:status
pkill -f cortex-improvement-loop.sh
```

Phases: **Build → Verify → Polish → Observe**. Polish target: `docs/goal-google-app-polish.md`.

See `docs/continuous-improvement-loop.md`.
